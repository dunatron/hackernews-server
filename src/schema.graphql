# Ok look into this. Might need to upgrade to bleeding/latest/next haha sort it out javascript
type Query {
  info: String!
  feed: [Link!]!
}

type Mutation {
  post(url: String!, description: String!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}
